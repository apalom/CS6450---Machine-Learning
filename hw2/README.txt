HOWTO: Run from Linux with run.sh.

-- perceptrons.py --

Defines all 4 perceptron variants, base data loader, and label accuracy functions.

# import libraries: pandas, numpy

-- hyper_parameters.py --

Defines CV data loader, define best hyper-parameters combinations over CV, training with best HP, plot learning curves, and evaluate on test data.

# import libraries: pandas, numpy, os.path, itertools, perceptrons